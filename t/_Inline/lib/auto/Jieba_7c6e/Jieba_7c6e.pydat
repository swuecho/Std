namespace : %
    classes : %
    functions : @
        jieba_tokenize_python
filtered : <<EOV
from jieba import tokenize

def jieba_tokenize_python(text):
    seg_list = tokenize(text, mode='search')
    return(list(seg_list))

EOV
